export default function TextBodyLeft({text}) {
    return (
        <div>
            <p className="text-base font-secondary text-neutral-200 mt-8 mb-3 pr-[10%] md:text-xl md:pt-[5%]">{text}</p>
        </div>
    )
}