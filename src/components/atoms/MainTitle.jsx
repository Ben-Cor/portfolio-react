export default function MainTitle({title, secondTitle}) {


    return(
        <div>
            <h1 className="text-[2rem] text-neutral-100 font-primary md:text-[4.2rem]">{title}</h1>
        </div>
    )
}